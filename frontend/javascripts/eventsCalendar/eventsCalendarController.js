var app = require('../app');

app.controller('EventsViewController', EventsViewController);


function EventsViewController() {
	var vm = this;
	// vm.events = 
	// some methods that will filter the content
}